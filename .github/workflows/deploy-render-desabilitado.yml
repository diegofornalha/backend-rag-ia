# Este workflow está desabilitado temporariamente
# Para habilitar:
# 1. Renomeie para deploy.yml
# 2. Descomente o código abaixo
# 3. Configure os secrets no GitHub (RENDER_API_KEY e RENDER_SERVICE_ID)

# name: Deploy
#
# on:
#   push:
#     branches: [main]
#
# env:
#   RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
#   RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to Render
#         run: |
#           curl -X POST \
#             "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
#             -H "accept: application/json" \
#             -H "authorization: Bearer $RENDER_API_KEY"
#
#       - name: Wait for deployment
#         run: |
#           echo "Aguardando deploy... (2 minutos)"
#           sleep 120
