{
  "name": "api-frontend-flask",
  "description": "Frontend e Backend Flask",
  "commands": {
    "Frontend (porta 2000)": {
      "command": "source .env && source .venv/bin/activate && pip install -r requirements_frontend.txt && python3 -m flask run --debug --port 2000",
      "description": "Inicia o servidor frontend na porta 2000"
    },
    "Backend (porta 3000)": {
      "command": "source .env && source .venv/bin/activate && pip install -r requirements_backend.txt && python3 -m flask run --debug --port 3000",
      "description": "Inicia o servidor backend na porta 3000"
    },
    "Instalar Todas Dependências": {
      "command": "source .venv/bin/activate && pip install -r requirements_frontend.txt && pip install -r requirements_backend.txt",
      "description": "Instala todas as dependências (frontend e backend)"
    }
  },
  "config": {
    "process-timeout": 0
  }
}
