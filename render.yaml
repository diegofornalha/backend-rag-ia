services:
  - type: web
    name: seu-servico
    env: docker
    dockerfilePath: ./backend_rag_ia/Dockerfile
    dockerCommand: python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
